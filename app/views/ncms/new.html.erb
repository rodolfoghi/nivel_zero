<% content_for :title, "New ncm" %>

<h1 class="font-bold text-4xl mbe-4">New ncm</h1>

<%= render "form", ncm: @ncm %>

<div class="inline-flex flex-wrap items-center gap mbs-2" style="--row-gap: .5rem">
  <%= link_to "Back to ncms", ncms_path, class: "btn" %>
</div>
